This code is the last step for plotting the fatbands. After a succesfull SIESTA run, the "fat" program must be run with the .EIG file (see the relative documentation). After
that, eigfat2plot must be run in order to obtain .dat files. 
After .dat files are obtained this executable can be run.
